# 🚀 ПОВНА ІНСТРУКЦІЯ ПО ВСТАНОВЛЕННЮ ТА ЗАПУСКУ

## ✅ Що вже реалізовано:

### Backend (Telegram Bot):
- ✅ Повна обробка команд `/start`, `/schedule`, `/notifications`, `/help`
- ✅ Збереження адреси користувача в базі даних
- ✅ Відображення графіків відключень з зображеннями
- ✅ Система автоматичних сповіщень:
  - Ранкові сповіщення (7:00) - графік на сьогодні
  - Вечірні сповіщення (18:00) - графік на завтра
  - Сповіщення про зміни в графіку (кожні 15 хв)
- ✅ Управління адресами (додавання, видалення, зміна)
- ✅ Увімкнення/вимкнення сповіщень
- ✅ Інтеграція з API Львівобленерго
- ✅ SQLite база даних з кешуванням

### Frontend (Web App):
- ✅ Красивий інтерфейс з темною темою
- ✅ Відображення статусу світла (є/немає) з анімацією
- ✅ Таймер зворотного відліку до включення/відключення
- ✅ Вибір адреси (місто, вулиця, будинок)
- ✅ Попередній перегляд графіку
- ✅ Збереження адреси в localStorage
- ✅ Відправка даних в Telegram бот
- ✅ Адаптивний дизайн для всіх екранів
- ✅ Кнопка зміни адреси

## 📦 КРОК 1: Підготовка

### 1.1 Встановіть Python (якщо ще не встановлено):
```bash
python --version  # Має бути 3.9+
```

### 1.2 Встановіть залежності бота:
```bash
cd /workspaces/lvivoblenergobot/bot
pip install -r requirements.txt
```

### 1.3 Перевірте requirements.txt:
Файл має містити:
```
python-telegram-bot>=20.7
aiohttp>=3.9.0
aiosqlite>=0.19.0
python-dotenv>=1.0.0
```

## 🤖 КРОК 2: Створення Telegram Бота

### 2.1 Створіть бота:
1. Відкрийте [@BotFather](https://t.me/BotFather) в Telegram
2. Надішліть `/newbot`
3. Введіть назву, наприклад: `Львівобленерго Помічник`
4. Введіть username, наприклад: `lvivoblenergo_helper_bot`
5. Збережіть отриманий **TOKEN**

### 2.2 Налаштуйте бота:
Надішліть BotFather:
```
/setdescription
# Введіть: Відстеження графіків відключень електроенергії у Львівській області

/setabouttext
# Введіть: Бот допоможе вам відстежувати графіки погодинних відключень, отримувати сповіщення про зміни та бути в курсі ситуації зі світлом.

/setcommands
start - Головне меню
schedule - Показати графік
notifications - Налаштування сповіщень
help - Допомога
```

## 🌐 КРОК 3: Розгортання Web App

### Варіант A: Firebase Hosting (Рекомендується)

#### 3.1 Встановіть Firebase CLI:
```bash
npm install -g firebase-tools
```

#### 3.2 Увійдіть в Firebase:
```bash
firebase login
```

#### 3.3 Ініціалізуйте проект:
```bash
cd /workspaces/lvivoblenergobot/webapp
firebase init hosting
```

Виберіть:
- Create a new project або використайте існуючий
- Public directory: `public`
- Single-page app: `No`
- Automatic builds: `No`

#### 3.4 Розгорніть:
```bash
firebase deploy
```

#### 3.5 Скопіюйте URL:
Після деплою ви отримаєте URL типу:
```
https://your-project-12345.web.app
```

### Варіант B: Vercel (Альтернатива)

```bash
cd /workspaces/lvivoblenergobot/webapp
npm install -g vercel
vercel login
vercel --prod
```

### Варіант C: Netlify

1. Перейдіть на [netlify.com](https://netlify.com)
2. Drag & Drop папку `webapp/public/`
3. Скопіюйте URL

## ⚙️ КРОК 4: Налаштування Bot Token

### 4.1 Створіть файл .env:
```bash
cd /workspaces/lvivoblenergobot/bot
cp .env.example .env
nano .env  # або будь-який інший редактор
```

### 4.2 Вставте ваші дані:
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
WEBAPP_URL=https://your-project-12345.web.app
LOE_API_BASE=https://power-api.loe.lviv.ua/api
LOE_MAIN_API_BASE=https://api.loe.lviv.ua/api
CHECK_INTERVAL=15
```

**Важливо:**
- Замініть `BOT_TOKEN` на ваш токен від BotFather
- Замініть `WEBAPP_URL` на URL вашого веб-додатку

## 🚀 КРОК 5: Запуск Бота

### 5.1 Запустіть бота:
```bash
cd /workspaces/lvivoblenergobot/bot
python main.py
```

Ви повинні побачити:
```
INFO - Database initialized
INFO - Notification service started
INFO - Starting bot...
```

### 5.2 Перевірте роботу:
1. Знайдіть вашого бота в Telegram
2. Натисніть `/start`
3. Натисніть "📍 Налаштувати адресу"
4. Оберіть місто, вулицю, будинок
5. Натисніть "💾 Зберегти адресу"

## ✅ КРОК 6: Тестування Функціоналу

### 6.1 Перевірте основні функції:
- [ ] `/start` - Відображається головне меню
- [ ] "Налаштувати адресу" - Відкривається Web App
- [ ] Вибір адреси - Працює пошук міста/вулиці
- [ ] "Зберегти адресу" - Адреса зберігається
- [ ] "Показати графік" - Відображається графік
- [ ] "Сповіщення" - Можна увімкнути/вимкнути

### 6.2 Перевірте Web App:
- [ ] Відображається статус світла (✅/❌)
- [ ] Працює таймер зворотного відліку
- [ ] Показується збережена адреса
- [ ] Показується група ГПВ
- [ ] Завантажується графік
- [ ] Кнопка "Змінити адресу" працює

### 6.3 Перевірте сповіщення:
1. Увімкніть сповіщення в боті
2. Зачекайте до 7:00 - має прийти графік на сьогодні
3. Зачекайте до 18:00 - має прийти графік на завтра
4. Бот перевіряє зміни кожні 15 хвилин

## 🔧 КРОК 7: Налаштування для Production

### 7.1 Використайте systemd (для Linux серверів):

Створіть файл `/etc/systemd/system/lvivoblenergo-bot.service`:
```ini
[Unit]
Description=Lvivoblenergo Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/lvivoblenergobot/bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустіть:
```bash
sudo systemctl enable lvivoblenergo-bot
sudo systemctl start lvivoblenergo-bot
sudo systemctl status lvivoblenergo-bot
```

### 7.2 Або використайте PM2 (Node.js):
```bash
npm install -g pm2
pm2 start bot/main.py --name lvivoblenergo-bot --interpreter python3
pm2 save
pm2 startup
```

### 7.3 Docker (опціонально):
Створіть `Dockerfile`:
```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY bot/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY bot/ .

CMD ["python", "main.py"]
```

Запустіть:
```bash
docker build -t lvivoblenergo-bot .
docker run -d --name lvivoblenergo-bot --restart unless-stopped \
  -v $(pwd)/bot/data:/app/data \
  --env-file bot/.env \
  lvivoblenergo-bot
```

## 📊 КРОК 8: Моніторинг

### 8.1 Логи бота:
```bash
# Якщо запущено через systemd
sudo journalctl -u lvivoblenergo-bot -f

# Якщо запущено через PM2
pm2 logs lvivoblenergo-bot

# Якщо запущено через Docker
docker logs -f lvivoblenergo-bot
```

### 8.2 Перевірка бази даних:
```bash
cd bot/data
sqlite3 users.db

# В SQLite консолі:
.tables
SELECT COUNT(*) FROM users;
SELECT COUNT(*) FROM user_addresses;
.quit
```

## 🎯 Що робити далі:

### Покращення статусу світла:
Зараз статус світла працює на шаблонній логіці. Для точного визначення потрібно:
1. Парсити зображення графіків (OCR)
2. Або отримати доступ до API з детальними даними
3. Або вручну вводити інтервали відключень

### Додаткові функції:
- Додати команду для ручного введення графіку
- Експорт графіка в календар
- Статистика відключень
- Порівняння план vs факт

## 🐛 Вирішення проблем:

### Бот не відповідає:
```bash
# Перевірте що бот запущений
ps aux | grep main.py

# Перевірте логи
tail -f bot.log

# Перезапустіть бота
pkill -f main.py
cd bot && python main.py
```

### Web App не відкривається:
1. Перевірте що WEBAPP_URL правильний
2. Перевірте що файли розгорнуті на хостингу
3. Відкрийте URL в браузері напряму

### Сповіщення не приходять:
1. Перевірте що сповіщення увімкнені в боті
2. Перевірте логи на наявність помилок
3. Переконайтесь що CHECK_INTERVAL встановлений правильно

## 📞 Підтримка

Якщо виникли проблеми:
1. Перевірте логи бота
2. Перевірте що всі змінні в .env встановлені правильно
3. Перевірте що API Львівобленерго доступні
4. Створіть issue на GitHub з описом проблеми

---

**Успіхів з запуском! ⚡**
